services:
  office-converter:
    build: .
    ports:
      - "9316:9316"
    volumes:
      - ./data:/tmp/conversions
    environment:
      - MAX_FILE_SIZE=50MB
      - TIMEOUT=120
    deploy:
      resources:
        limits:
          cpus: '1.5'
          memory: 1G