; KBot 4.0 空白 Schema 初始化范围。
; platform_core 是必建基础层，脚本会自动创建，不在此处配置。
; 数据库地址和凭据仍由 configuration/ 与环境变量提供。

[services]
model_serving = true
knowledge_core = true
agent_runtime = true

; aiops_agent 在步骤 1 DDL 完成后加入可选列表；步骤 0 不创建 OPS 表。
