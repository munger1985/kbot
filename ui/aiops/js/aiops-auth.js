(function(){"use strict";const key="kbot.aiops.session.v1";const base=()=>String(globalThis.KBOT_UI_CONFIG?.mainApiBaseUrl||"").replace(/\/+$/,"");const load=()=>{try{return JSON.parse(sessionStorage.getItem(key)||"null")}catch(_){return null}};const save=v=>(sessionStorage.setItem(key,JSON.stringify(v)),v);const clear=()=>sessionStorage.removeItem(key);async function raw(path,options={},token=""){const headers={Accept:"application/json","X-Request-ID":crypto.randomUUID(),...(options.headers||{})};if(token)headers.Authorization=`Bearer ${token}`;if(options.body)headers["Content-Type"]="application/json";const response=await fetch(`${base()}${path}`,{...options,cache:"no-store",headers});const text=await response.text();let payload=null;try{payload=text?JSON.parse(text):null}catch(_){payload=text}if(!response.ok){const error=new Error(payload?.detail?.message||payload?.detail||payload?.message||`请求失败（HTTP ${response.status}）`);error.status=response.status;error.payload=payload;throw error}return payload}async function login(body){return save(await raw("/api/v1/auth/apps/aiops/login",{method:"POST",body:JSON.stringify(body)}))}async function request(path,options={}){const session=load();if(!session?.access_token){location.replace("./login.html");throw new Error("请先登录 AIOps")}try{return await raw(path,options,session.access_token)}catch(error){if(error.status===401)clear();throw error}}globalThis.KBotAIOpsAuth={load,clear,login,request}})();
