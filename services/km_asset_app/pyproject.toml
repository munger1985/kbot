[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kbot-km-asset-app"
version = "4.0.0"
description = "KBot KM Asset 同步、问文和问数应用"
requires-python = ">=3.10"
dependencies = [
  "aiohttp==3.13.3",
  "fastapi==0.128.0",
  "fastapi-offline==1.7.6",
  "kbot-platform-core==4.0.0",
  "kbot-platform-clients==4.0.0",
  "loguru==0.7.3",
  "pydantic==2.12.5",
  "SQLAlchemy==2.0.46",
  "uvicorn==0.49.0",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["km_asset_app*"]
